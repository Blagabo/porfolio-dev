---
import { Icon } from "astro-icon/components"
import { siteConfig } from "../data/site"
---

<footer
	class="border-border-primary mt-16 border-t bg-bg-secondary py-12 text-text-primary dark:bg-bg-secondary"
>
	<div
		class="mx-auto flex max-w-7xl flex-col justify-between gap-8 px-4 sm:px-6 md:flex-row lg:px-8"
	>
		<div>
			<a href="#home" class="font-heading text-xl font-bold text-text-primary">{siteConfig.name}</a>
			<p class="mt-2 max-w-md text-text-secondary">
				{siteConfig.heroDescription}
			</p>
			<p class="mt-2 flex items-center gap-2 text-text-primary">
				<Icon name="fa6-solid:location-dot" />
				{siteConfig.heroLocation}
			</p>
			<a
				href={`mailto:${siteConfig.contactEmail}`}
				class="mt-2 inline-flex items-center gap-2 text-text-primary"
			>
				<Icon name="fa6-regular:envelope" />
				{siteConfig.contactEmail}
			</a>
		</div>
		<div class="flex flex-col items-start gap-4 md:items-end">
			<div class="flex flex-wrap justify-end gap-2">
				{siteConfig.collabTags.map((tag) => <span class="tech-pill text-xs">{tag}</span>)}
			</div>
			<div class="flex gap-4">
				{
					siteConfig.social.map((item) => (
						<a
							href={item.url}
							target="_blank"
							rel="noreferrer"
							class="hover:text-third text-text-primary transition-colors dark:text-text-primary"
						>
							<Icon name={item.icon} class="text-2xl" aria-label={item.label} />
						</a>
					))
				}
			</div>
		</div>
	</div>
	<div
		class="border-border-primary mt-8 border-t pt-6 text-center text-sm text-text-secondary dark:text-text-secondary"
	>
		© {new Date().getFullYear()}
		{siteConfig.name}. All rights reserved.
	</div>
</footer>
